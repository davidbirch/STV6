      <ul class="nav navbar-nav navbar-right">
        <% Region.all.each do |region| %>
          <% if @region.name == region.name %>
            <li class="active"><%= link_to (region.name+"<span class=\"sr-only\">(current)</span>").html_safe, ("/guides/"+region.friendly_id) %></li>
          <% else %>
            <li><%= link_to region.name, ("/guides/"+region.friendly_id) %></li>
          <% end %>
        <% end %>
      </ul> <!-- /nav navbar-nav navbar-right-->
